export class Properties {
    public serverName: string;
    public serverContent: string;
    constructor(sn: string, sc: string) {
        this.serverName = sn;
        this.serverContent = sc;
    }
 }
